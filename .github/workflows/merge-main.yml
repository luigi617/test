name: Merge main
on:
  push:
    branches: [ main ]
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout for local action reference
        uses: actions/checkout@v4
      - name: Merge main
        uses: ./.github/actions/merge-main
        with:
          ci-user-pat: ${{ secrets.CI_USER_PAT }}
          exempt-branches: ""
